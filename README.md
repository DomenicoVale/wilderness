# Wilderness

Wilderness is an in-house browser extension that injects a React UI into any page.

TODO:
- simple network interceptor / proxy and repeater
- per-page project configuration, with per-page tools, per-page api testing toolsets
- reCAPTCHA tester, maybe as subset of network interceptor, or just as an additional pre-configured tool

